{
  "name": "gacha-game-types",
  "version": "1.0.0",
  "private": true,
  "description": "Type definitions for processed gacha-game data models (HSR, GI, and more)",
  "scripts": {
    "build": "tsc && tsc-alias"
  },
  "devDependencies": {
    "type-fest": "^4.41.0",
    "typescript": "^5.3.3"
  }
}
